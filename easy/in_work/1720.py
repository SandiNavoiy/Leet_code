class Solution:
    def decode(self, encoded: list[int], first: int) -> list[int]:
        ''''''
        new = [first]

        return new
encoded = [1,2,3]
first = 1
s = Solution()
print(s.decode(encoded, first))